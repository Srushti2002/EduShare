# 1. Base image
FROM node:18

# 2. Set working directory
WORKDIR /app

# 3. Copy everything
COPY . .

# 4. Install dependencies
RUN npm install

# 5. Build the app
RUN npm run build

# 6. Expose the port Vite preview uses
EXPOSE 4173

# 7. Start Vite preview server (bind to 0.0.0.0 to allow external access)
CMD ["npm", "run", "preview", "--", "--host", "0.0.0.0"]
